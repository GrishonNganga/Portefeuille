/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "res.cloudinary.com",
            "cdn.aglty.io",
            "media.licdn.com"
        ]
    },
};

export default nextConfig;
